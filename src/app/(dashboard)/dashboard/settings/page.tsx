import SettingPage from "@/features/(dashboard)/settings";
import React from "react";

const Settings = () => {
  return (
    <div>
      <SettingPage />
    </div>
  );
};

export default Settings;
