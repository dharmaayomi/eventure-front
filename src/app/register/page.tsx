import RegisterPage from "@/features/(root)/auth/register";
import React from "react";

const Register = () => {
  return (
    <div>
      <RegisterPage />
    </div>
  );
};

export default Register;
