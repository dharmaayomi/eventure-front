import Image from "next/image";
import { Modal } from "@/components/ui/modal"; // Pastikan ini support onClickOutside / backdrop click
import { Button } from "@/components/ui/button";

type PaymentProofModalProps = {
  isOpen: boolean;
  closeModal: () => void;
  paymentProofUrl: string;
  onAccept: () => void;
  onReject: () => void;
};

const PaymentProofModal: React.FC<PaymentProofModalProps> = ({
  isOpen,
  closeModal,
  paymentProofUrl,
  onAccept,
  onReject,
}) => {
  return (
    <Modal isOpen={isOpen} onClose={closeModal} className="m-4 max-w-[700px]">
      <div
        className="no-scrollbar relative h-[100vh] w-full max-w-[700px] overflow-y-auto rounded-3xl bg-white p-4 lg:p-11 dark:bg-gray-900"
        role="dialog"
        aria-modal="true"
      >
        <div className="px-2 pr-14">
          <h4 className="mb-2 text-2xl font-semibold text-gray-800 dark:text-white/90">
            See Payment Proof
          </h4>
          <p className="mb-6 text-sm text-gray-500 lg:mb-7 dark:text-gray-400">
            Review the payment proof submitted by the user.
          </p>
        </div>

        <div className="flex justify-center">
          {paymentProofUrl ? (
            <Image
              src={paymentProofUrl}
              alt="Payment Proof"
              width={600}
              height={400}
              className="rounded-xl border shadow"
              objectFit="contain"
            />
          ) : (
            <p className="text-center text-gray-500 dark:text-gray-300">
              No payment proof available.
            </p>
          )}
        </div>

        <div className="mt-6 flex items-center justify-end gap-3 px-2">
          <Button size="sm" variant="outline" onClick={onReject}>
            Reject
          </Button>
          <Button size="sm" onClick={onAccept}>
            Accept
          </Button>
        </div>
      </div>
    </Modal>
  );
};

export default PaymentProofModal;
